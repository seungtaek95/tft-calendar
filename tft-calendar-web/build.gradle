dependencies {
    implementation project(':tft-calendar-core')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}

bootJar {
    enabled = true
    archiveFileName.set("tft-calendar-web.jar")
}
